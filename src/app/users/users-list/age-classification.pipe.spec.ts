/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AgeClassificationPipe } from './age-classification.pipe';

describe('Pipe: AgeClassificatione', () => {
  it('create an instance', () => {
    let pipe = new AgeClassificationPipe();
    expect(pipe).toBeTruthy();
  });
});
